appId: com.codeall.app
productName: CodeAll
directories:
  output: dist
files:
  - out/**/*
  - prisma/**/*
  - node_modules/**/*
  - package.json
win:
  target: nsis
  # icon: resources/icon.ico  # TODO: Add proper .ico file (must be ICO format, not SVG)
publish: null
