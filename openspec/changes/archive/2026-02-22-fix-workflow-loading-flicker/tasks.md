## 1. 状态与请求归属治理

- [x] 1.1 盘点流程图点击到详情渲染链路中的现有 loading 状态入口，统一为 `idle/loading/ready/error` 四态并消除重复状态源。
- [x] 1.2 在工作流详情加载入口实现“请求代次（request token）+ 当前选择归属校验”，确保旧响应不会覆盖最新选择。
- [x] 1.3 为详情加载失败路径补齐可恢复行为（显式 error 状态与重试触发），避免界面长期停留在“加载工作流…”。

## 2. 渲染稳定性与交互连续性

- [x] 2.1 调整详情面板切换为“稳定容器 + 渐进内容更新”，禁止加载期间容器级卸载和空白闪屏。
- [x] 2.2 在切换期间保留上一次有效内容或稳定骨架占位，并增加“正在加载新工作流”的明确提示。
- [x] 2.3 校准流程图选中态与详情面板状态联动，保证快速连续点击时最终展示始终对应最后一次选择。

## 3. IPC/事件链路一致性保障

- [x] 3.1 校验工作流详情查询返回的身份字段与状态字段，确保前端可据此执行响应归属判断。
- [x] 3.2 对工作流恢复与续跑相关查询增加终态收敛约束（ready 或 explicit failure），避免无界 loading。
- [x] 3.3 清理或收敛可能触发重复刷新/重复订阅的事件桥接路径，防止状态抖动放大为闪烁。

## 4. 测试与回归验证

- [x] 4.1 新增/更新前端测试：覆盖“快速连续点击 + 响应乱序返回”场景，断言 last-selection-wins 行为。
- [x] 4.2 新增/更新集成测试：覆盖慢响应、失败重试、恢复后查询场景，断言加载状态可收敛且不悬挂。
- [x] 4.3 增加渲染稳定性断言（无空白中间帧/无重复闪烁）并在本地测试开发服务器场景回归验证。