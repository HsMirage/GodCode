# CodeAll v1.0.0 发布说明

> 发布日期：2026-03-07
> 版本：`v1.0.0`
> 发布形态：Beta / 手动更新

## 本版重点

- **API / IPC 类型闭环**：补齐共享 IPC 合同，恢复 `pnpm typecheck` 通过状态。
- **Updater 主链补全**：将 updater handler 收敛到 `src/main/ipc/handlers/updater.ts`，并在 dev 模式下可验证前后端联通。
- **工作台体验完善**：任务面板拆分、会话/流式状态收敛、中文 UI 文本常量化、Agent 下拉自适应展开。
- **构建与交付收尾**：完成 macOS 安装包构建，输出 DMG 与 blockmap。

## 构建产物

- `dist/CodeAll-1.0.0.dmg`
- `dist/CodeAll-1.0.0.dmg.blockmap`
- `dist/CodeAll-1.0.0-arm64.dmg`
- `dist/CodeAll-1.0.0-arm64.dmg.blockmap`

## 验证摘要

- `pnpm test`：`1027/1027` 通过
- `pnpm typecheck`：通过
- `pnpm lint`：`0 errors / 97 warnings`
- `pnpm build:mac`：通过

## 交付说明

- README 已补充安装包获取、首次启动引导和基本使用流程。
- `docs/user-guide/configuration.md` 已覆盖 API Key、模型配置、Agent 绑定与常见问题。
- 当前 `electron-builder.yml` 保持 `publish: null`，因此更新采用**手动发布**模式。

## 已知说明

- macOS 安装包当前未签名；在部分系统上首次打开可能需要手动放行。
- 已完成 `dist/win-unpacked` 打包，但 NSIS 安装器在当前 arm64 macOS 主机上因 `makensis` 启动错误（`spawn ... error -86`）未能生成 EXE；Windows 安装包需在兼容环境中完成最终验证。
- “全新 macOS 环境安装并完成首次对话流程”仍建议作为发布前最后一轮人工验收。
